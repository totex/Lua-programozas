-- A Lua dinamikus nyelv, nincs szükség megadni a változók típusát.

--nil type
semmi = nil -- None, null, nullptr

--number type
szam1 = 24        --integers
szam2 = 3.141592  --floating point numbers
szam3 = 4.57e-3   --scientific notation: 4.57 x 10^-3 = 0.00457

--string
szoveg1 = "Hello!"
szoveg2 = 'Jó napot!'

--boolean type
szoke = false
barna = true

--function type
myFunc = function() end

--table type (arrays, dictionaries, objects)
myTable = {1, 2, 3, 4}

--userdata type - Tetszőleges C adatot képvisel.
--The type userdata is provided to allow arbitrary C data to be stored in Lua variables.
--A userdata value is a pointer to a block of raw memory.




